<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:gravity="center"
    android:orientation="horizontal"
    style="@style/titlebar_menu">

    <TextView
        style="@style/titlebar_button"
        android:layout_width="90dp"
        android:layout_height="fill_parent"
        android:layout_alignParentStart="true"
        android:gravity="center"
        android:id="@+id/leftButtonOptionsTitlebar"
        android:layout_centerHorizontal="true"
        android:textSize="12sp" />

    <TextView
        style="@style/titlebar_button"
        android:layout_width="90dp"
        android:layout_height="fill_parent"
        android:layout_alignParentEnd="true"
        android:gravity="center"
        android:id="@+id/rightButtonOptionsTitlebar"
        android:textSize="12sp" />

    <TextView
        style="@style/titlebar_title"
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:layout_centerHorizontal="true"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:text="@string/app_name"
        android:id="@+id/titleOptionsTitlebar"
        android:textSize="18sp"
        android:layout_toEndOf="@id/leftButtonOptionsTitlebar"
        android:layout_toStartOf="@id/rightButtonOptionsTitlebar"
        android:textStyle="bold" />
</RelativeLayout>
